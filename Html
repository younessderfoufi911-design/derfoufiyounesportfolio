<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Younes Derfoufi - Élève Ingénieur Géomètre Topographe</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; background-color: #101010; color: #e0e0e0; line-height: 1.6; position: relative; overflow-x: hidden; }

        /* Calm red background gradient */
        .bg-image {
            position: fixed; top: 0; left: 0; width: 100%; height: 120%;
            background: linear-gradient(135deg, #2b0f10 0%, #3a1213 50%, #5a1f1f 100%);
            background-size: cover; background-position: center center; background-repeat: no-repeat; z-index: -2; transition: opacity 0.3s ease, transform 0.2s ease-out;
            will-change: transform, opacity;
        }

        .bg-overlay {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(to bottom, rgba(16,12,12,0.75), rgba(16,12,12,0.95)); z-index: -1; transition: background 0.3s ease;
        }

        nav { position: fixed; top: 0; width: 100%; background-color: rgba(16,12,12,0.95); backdrop-filter: blur(10px); z-index: 1000; border-bottom: 1px solid rgba(70,20,20,0.6); }
        nav ul { list-style: none; display: flex; justify-content: center; padding: 1.2rem; gap: 2.5rem; }
        nav a { color: #e8dede; text-decoration: none; font-weight: 500; letter-spacing: 0.5px; transition: color 0.25s ease; font-size: 0.95rem; position: relative; padding: 0.5rem 0; }

        nav a::before { content: ''; position: absolute; top: -6px; left: 0; width: 0; height: 2px; background: #c47a7a; transition: width 0.4s ease 0.0s; }
        nav a::after { content: ''; position: absolute; inset: -8px; border-radius: 8px; background: transparent; box-shadow: 0 0 0 0 rgba(196,122,122,0); transition: box-shadow 0.4s ease; }
        nav a:hover { color: #f1dede; }
        nav a:hover::before { width: 100%; }

        header { height: 100vh; display: flex; flex-direction: column; justify-content: center; align-items: center; text-align: center; padding: 2rem; position: relative; }

        h1 { font-size: 3.5rem; font-weight: 800; margin-bottom: 1rem; letter-spacing: -1px; cursor: pointer; position: relative; color: transparent; }
        /* Liquid glass name text */
        .name-text {
            background-image:
                radial-gradient(1200px 400px at 10% 20%, rgba(255,255,255,0.07), transparent 15%),
                linear-gradient(90deg, rgba(255,235,235,1) 0%, rgba(220,120,120,1) 25%, rgba(170,70,70,1) 55%, rgba(230,140,140,1) 80%, rgba(255,240,240,1) 100%);
            background-size: 300% 100%;
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            -webkit-text-stroke: 0.6px rgba(255,255,255,0.06);
            text-shadow: 0 6px 20px rgba(160,50,50,0.12), inset 0 0 10px rgba(255,255,255,0.02);
            transition: transform 0.35s ease;
            display: inline-block;
            padding: 0.1rem 0.2rem;
            border-radius: 8px;
            position: relative;
            overflow: visible;
        }

        /* animated sheen that creates liquid/glass motion */
        .name-text::before {
            content: '';
            position: absolute;
            inset: -2px;
            background: linear-gradient(120deg, rgba(255,255,255,0.06), rgba(255,255,255,0.02) 20%, rgba(255,255,255,0.06) 40%, transparent 60%);
            filter: blur(10px);
            mix-blend-mode: screen;
            opacity: 0.9;
            pointer-events: none;
            transform: translateX(-40%);
            transition: transform 0.6s cubic-bezier(.2,.9,.2,1);
            border-radius: 8px;
            z-index: -1;
        }

        .name-text.liquid { animation: liquidMove 6s linear infinite; background-size: 180% 100%; }
        @keyframes liquidMove {
            0% { background-position: 0% 50%; }
            25% { background-position: 50% 40%; }
            50% { background-position: 100% 50%; }
            75% { background-position: 50% 60%; }
            100% { background-position: 0% 50%; }
        }

        .glow { filter: drop-shadow(0 0 6px rgba(180,90,90,0.25)); transition: filter 0.4s ease; }
        .glow-active { animation: glow 3s ease-in-out infinite; }
        @keyframes glow { 0%,100% { filter: drop-shadow(0 0 6px rgba(180,90,90,0.25)); } 50% { filter: drop-shadow(0 0 20px rgba(200,100,100,0.45)); } }

        .subtitle { font-size: 1.3rem; color: #c9bcbc; margin-bottom: 2rem; font-weight: 300; }

        .contact-info { display: flex; gap: 2rem; margin-top: 2rem; flex-wrap: wrap; justify-content: center; }
        .contact-item { display:flex; align-items:center; gap:0.5rem; color:#d4cfcf; font-size:0.95rem; }

        section { max-width: 1100px; margin: 0 auto; padding: 5rem 2rem; position: relative; z-index: 1; }
        h2 { font-size: 2.5rem; margin-bottom: 3rem; text-align: center; color: #ffecec; font-weight: 600; }

        .about-content { background-color: rgba(26,12,12,0.88); padding: 3rem; border-radius: 12px; border:1px solid rgba(70,20,20,0.35); line-height:1.8; backdrop-filter: blur(10px); }
        .about-content p { margin-bottom: 1.5rem; color:#d8cfcf; font-size:1.05rem; }

        .clickable-name { color: #dca6a6; cursor: pointer; transition: all 0.3s ease; text-decoration: underline; text-decoration-color: transparent; }
        .clickable-name:hover { text-decoration-color: #dca6a6; text-shadow: 0 0 10px rgba(220,166,166,0.45); }

        .easter-egg { display: none; margin-top: 2rem; padding: 1.5rem; background: linear-gradient(135deg, rgba(180,100,100,0.06), rgba(120,60,60,0.03)); border-left: 3px solid #c47a7a; border-radius: 8px; }
        .easter-egg.show { display: block; animation: fadeIn 0.6s ease; }
        @keyframes fadeIn { from { opacity:0; transform: translateY(-8px);} to { opacity:1; transform: translateY(0);} }

        /* rest of styles unchanged but color accents adjusted to red tones */
     .skills-bubble-container {
    display: grid; /* Use grid layout */
    grid-template-columns: repeat(2, 1fr); /* Two columns */
    gap: 1rem; /* Space between bubbles */
    margin: 2rem 0; /* Margin for the grid */
}

.skill-bubble {
    width: 80px; /* Set a fixed width for circular shape */
    height: 80px; /* Set a fixed height for circular shape */
    display: flex; /* Center content */
    justify-content: center; /* Center content */
    align-items: center; /* Center content */
    border-radius: 50%; /* Make it circular */
    padding: 0; /* Remove padding */
    font-size: 0.85rem; /* Font size */
    background: linear-gradient(135deg, rgba(90,40,40,0.1), rgba(70,30,30,0.05)); /* Background */
    border: 1px solid rgba(100,40,40,0.2); /* Border */
    transition: transform 0.2s ease, box-shadow 0.2s ease; /* Transition effects */
    position: relative; /* Position for floating effect */
    animation: float 3s ease-in-out infinite; /* Floating animation */
}


@keyframes float {
    0%, 100% {
        transform: translateY(0); /* Original position */
    }
    50% {
        transform: translateY(-5px); /* Floating effect */
    }
}

.skill-bubble:hover {
    box-shadow: 0 0 20px rgba(180,90,90,0.5); /* Glowing effect on hover */
    background: linear-gradient(135deg, rgba(100,45,45,0.15), rgba(80,35,35,0.05)); /* Softer hover background */
}

        /* keep responsive rules */
        @media (max-width: 768px) {
            h1 { font-size: 2.2rem; }
            .subtitle { font-size: 1.05rem; }
            nav ul { flex-wrap: wrap; gap: 1rem; }
            .projects-container { gap: 1rem; }
            .skills-bubble-container { grid-template-columns: repeat(2, 1fr); gap: 1rem; max-width: 100%; }
            .skill-bubble { padding: 1rem; }
            .skill-bubble span { font-size: 0.8rem; }
        }
    </style>
</head>
<body>
    <div class="bg-image" id="bgImage"></div>
    <div class="bg-overlay" id="bgOverlay"></div>

    <nav>
        <ul>
            <li><a href="#accueil">Accueil</a></li>
            <li><a href="#apropos">À propos</a></li>
            <li><a href="#projets">Mes projets</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
    </nav>

    <header id="accueil">
        <h1 id="nameTitle" onclick="goToContact()"><span id="nameText" class="name-text glow"></span></h1>
        <p class="subtitle">Élève Ingénieur Géomètre Topographe</p>
        <div class="contact-info">
            <div class="contact-item"><span>📧</span><span>younesderfoufi111@gmail.com</span></div>
            <div class="contact-item"><span>📍</span><span>Meknès, Maroc</span></div>
        </div>
    </header>

    <section id="apropos">
        <h2>À propos</h2>
        <div class="about-content">
            <p>Élève ingénieur géomètre topographe polyvalent, actuellement en formation en Sciences Géomatiques et Ingénierie Topographique à l'IAV Hassan II. Je dispose de compétences significatives dans les Systèmes d'Information Géographique (SIG), l'analyse de performances des solutions de bases de données spatiales et l'automatisation des processus géospatiaux.</p>
            <p>Mon parcours académique inclut une formation solide en classes préparatoires MPSI-MP (2021-2023) et un baccalauréat Sciences Mathématiques option Français. J'ai enrichi mes compétences théoriques à travers plusieurs stages professionnels, notamment au Service Cadastre et Conservation Foncière (ANCFCC) et au sein de la société MAPTOPO SARL.</p>
            <p>Passionné par l'innovation technologique dans le domaine de la géomatique, je m'investis particulièrement dans le benchmarking des solutions SIG et l'optimisation des workflows géospatiaux. Je suis <span class="clickable-name" id="clickableName">Younes Derfoufi</span>, et je serais ravi de collaborer avec vous sur vos projets.</p>

            <div class="easter-egg" id="easterEgg"><p id="easterEggText"></p></div>

            <div class="skills-chart">
               <div class="skills-bubble-container" id="bubbleContainer">
    <div class="skill-bubble" data-tip="Conception et déploiement de cartes web interactives">
        <span>Web Mapping</span>
        <div class="tooltip" id="tooltip1"></div>
    </div>
    <div class="skill-bubble" data-tip="Analyses spatiales avancées">
        <span>Analyse Spatiale</span>
        <div class="tooltip" id="tooltip2"></div>
    </div>
    <div class="skill-bubble" data-tip="Gestion et optimisation de BD spatiales">
        <span>Bases de Données Spatiales</span>
        <div class="tooltip" id="tooltip3"></div>
    </div>
    <div class="skill-bubble" data-tip="Levés terrain et traitements topographiques">
        <span>Topographie de Terrain</span>
        <div class="tooltip" id="tooltip4"></div>
    </div>
    <div class="skill-bubble" data-tip="Traitement et orthorectification d'images">
        <span>Photogrammétrie</span>
        <div class="tooltip" id="tooltip5"></div>
    </div>
    <div class="skill-bubble" data-tip="Automatisation des workflows géospatiaux">
        <span>Automatisation</span>
        <div class="tooltip" id="tooltip6"></div>
    </div>
</div>
            </div>

            <div class="skills-grid">
                <div class="skill-category"><h3>Compétences Topographiques</h3><ul><li>Levés GNSS & Station totale</li><li>Nivellement de précision</li><li>Photogrammétrie par drone</li><li>Calculs de terrassement</li><li>Profils en long/travers</li></ul></div>
                <div class="skill-category"><h3>Compétences SIG</h3><ul><li>Analyse spatiale avancée</li><li>Cartographie thématique</li><li>Gestion de BD spatiales</li><li>Web Mapping</li><li>Automatisation workflows</li></ul></div>
                <div class="skill-category"><h3>Logiciels Topographie</h3><ul><li>Trimble Business Center</li><li>Leica Geo Office</li><li>Covadis & AutoCAD</li><li>Instruments GNSS</li><li>Stations totales</li></ul></div>
                <div class="skill-category"><h3>Logiciels SIG</h3><ul><li>QGIS</li><li>PostGIS</li><li>DuckDB Spatial</li><li>ArcGIS (notions)</li></ul></div>
                <div class="skill-category"><h3>Logiciels Télédétection</h3><ul><li>ERDAS Imagine</li><li>Mozaic Pro</li><li>Agisoft Metashape</li><li>Pix4D</li></ul></div>
                <div class="skill-category"><h3>Programmation & Outils</h3><ul><li>Python (géospatial)</li><li>SQL avancé</li><li>Model Builder</li><li>Git & GitHub</li></ul></div>
                <div class="skill-category"><h3>Modélisation 3D</h3><ul><li>AutoCAD 3D</li><li>Modélisation terrain</li><li>MNT/MNS</li><li>Visualisation 3D</li></ul></div>
                <div class="skill-category"><h3>Langues</h3><ul><li>Arabe (Natif)</li><li>Français (Courant)</li><li>Anglais (Professionnel)</li></ul></div>
            </div>
        </div>
    </section>

    <section id="projets">
        <h2>Mes projets</h2>
        <div class="projects-container" id="projectsList"></div>
        <button class="add-project-btn" id="addProjectBtn" onclick="openModal()">+ Ajouter un projet</button>
    </section>

    <section id="contact">
        <h2>Contactez-moi</h2>
        <div class="contact-content">
            <p>Intéressé par une collaboration ou souhaitez en savoir plus sur mon parcours ?</p>
            <div class="contact-details">
                <div class="contact-detail-item"><span>📧 Email:</span><span>younesderfoufi111@gmail.com</span></div>
                <div class="contact-detail-item"><span>📱 Téléphone:</span><span id="phoneNumber">+212 6 XX XX XX XX</span></div>
            </div>
            <a href="https://www.linkedin.com/in/younes-derfoufi" target="_blank" class="linkedin-btn" id="linkedinBtn">Voir mon profil LinkedIn</a>
        </div>
    </section>

    <footer><p>&copy; 2025 Younes Derfoufi - Élève Ingénieur Géomètre Topographe</p></footer>

    <button class="admin-toggle" id="adminToggleBtn" onclick="toggleAdmin()">Mode Admin</button>

    <div class="modal" id="projectModal">
        <div class="modal-content">
            <h3>Ajouter un projet</h3>
            <input type="text" id="projectTitle" placeholder="Titre du projet">
            <input type="text" id="projectDesc" placeholder="Description du projet">
            <input type="text" id="projectLink" placeholder="Lien vers le PDF (optionnel)">
            <div class="modal-buttons">
                <button class="save-btn" onclick="addProject()">Enregistrer</button>
                <button class="cancel-btn" onclick="closeModal()">Annuler</button>
            </div>
        </div>
    </div>

    <script>
        const bgImage = document.getElementById('bgImage');
        const bgOverlay = document.getElementById('bgOverlay');

        function setBackgroundImage(url) {
            if (!url) return;
            bgImage.style.backgroundImage = `url('${url}')`;
            bgImage.style.backgroundSize = 'cover';
            bgImage.style.backgroundPosition = 'center center';
            bgImage.style.opacity = '1';
        }

        window.addEventListener('scroll', () => {
            const scrolled = window.pageYOffset;
            const maxScroll = Math.max(document.documentElement.scrollHeight - window.innerHeight, 1);
            const scrollPercent = Math.min(scrolled / maxScroll, 1);
            const newOpacity = 0.7 + (scrollPercent * 0.25);
            bgOverlay.style.background = `linear-gradient(to bottom, rgba(16,12,12,${newOpacity}), rgba(16,12,12,0.95))`;
            const translateY = Math.round(scrolled * 0.2);
            bgImage.style.transform = `translateY(-${translateY}px)`;
        });

        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                const href = this.getAttribute('href');
                if (href.startsWith('#')) {
                    e.preventDefault();
                    const target = document.querySelector(href);
                    if (target) target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });

          const nameTextEl = document.getElementById('nameText');
        const fullName = 'YOUNES DERFOUFI';
        function typeText(el, text, speed = 80, callback) {
            el.textContent = '';
            let i = 0;
            const id = setInterval(() => {
                el.textContent += text.charAt(i);
                i++;
                if (i >= text.length) { clearInterval(id); if (callback) callback(); }
            }, speed);
        }

        window.addEventListener('load', () => {
            // Start type effect then enable liquid glass animation
            typeText(nameTextEl, fullName, 70, () => {
                nameTextEl.classList.add('glow-active');
                // enable the liquid glass animated background once typed
                nameTextEl.classList.add('liquid');
            });
        });

        function goToContact() { document.getElementById('contact').scrollIntoView({ behavior: 'smooth', block: 'start' }); }

        const clickableName = document.getElementById('clickableName');
        const easterEggEl = document.getElementById('easterEgg');
        const easterEggTextEl = document.getElementById('easterEggText');
        const easterEggMessage = "Je suis également producteur de musique dans le style EDM, et cofondateur de l'agence Framopia, spécialisée en photographie et vidéographie. Merci pour cette découverte approfondie sur moi — maintenant, on peut collaborer dans encore plus de domaines ! 🎶📸";
        let easterSeen = false;

        clickableName.addEventListener('click', () => {
            if (easterSeen) return;
            easterSeen = true;
            easterEggEl.classList.add('show');
            typeText(easterEggTextEl, easterEggMessage, 24);
        });

        const ADMIN_KEY = 'younes_is_admin_v1';
        const addProjectBtn = document.getElementById('addProjectBtn');
        const adminToggleBtn = document.getElementById('adminToggleBtn');
        const ADMIN_PASSWORD = 'framopiaAdmin2025';

        function setAdminState(state) {
            if (state) {
                addProjectBtn.classList.add('admin-visible');
                adminToggleBtn.classList.add('logged');
                adminToggleBtn.textContent = 'Mode Admin (actif)';
            } else {
                addProjectBtn.classList.remove('admin-visible');
                adminToggleBtn.classList.remove('logged');
                adminToggleBtn.textContent = 'Mode Admin';
            }
        }

        function toggleAdmin() {
            const currentlyAdmin = adminToggleBtn.classList.contains('logged');
            if (!currentlyAdmin) {
                const pass = prompt('Entrez le mot de passe administrateur :');
                if (pass === null) return;
                if (pass === ADMIN_PASSWORD) {
                    setAdminState(true);
                    alert('Mode administrateur activé');
                } else {
                    alert('Mot de passe incorrect');
                }
            } else {
                if (confirm('Voulez-vous désactiver le mode administrateur ?')) {
                    setAdminState(false);
                    alert('Mode administrateur désactivé');
                }
            }
        }

        let projects = [];
        function renderProjects() {
            const container = document.getElementById('projectsList');
            if (projects.length === 0) {
                container.innerHTML = `<div class="project-item"><div class="project-info"><h3>Comparaison Avancée DuckDB vs PostGIS</h3><p>Analyse comparative des solutions DuckDB et PostGIS dans différents scénarios de requêtes SQL spatiales</p></div><a href="#" class="download-btn" onclick="alert('Lien non disponible'); return false;">Télécharger PDF</a></div>`;
                return;
            }
            container.innerHTML = projects.map((p, idx) => `
                <div class="project-item">
                    <div class="project-info">
                        <h3>${p.title}</h3>
                        <p>${p.desc}</p>
                    </div>
                    <a href="${p.link || '#'}" class="download-btn" ${!p.link ? 'onclick="alert(\'Lien non disponible\'); return false;"' : 'target="_blank"'}>Télécharger PDF</a>
                </div>`).join('');
        }

        function openModal() { document.getElementById('projectModal').style.display = 'flex'; }
        function closeModal() { 
            document.getElementById('projectModal').style.display = 'none'; 
            document.getElementById('projectTitle').value = ''; 
            document.getElementById('projectDesc').value = ''; 
            document.getElementById('projectLink').value = ''; 
        }
        function addProject() {
            if (!adminToggleBtn.classList.contains('logged')) { 
                alert('Seul l\'administrateur peut ajouter des projets'); 
                return; 
            }
            const title = document.getElementById('projectTitle').value.trim();
            const desc = document.getElementById('projectDesc').value.trim();
            const link = document.getElementById('projectLink').value.trim();
            if (!title || !desc) { alert('Veuillez remplir le titre et la description du projet'); return; }
            projects.unshift({ title, desc, link }); 
            renderProjects(); 
            closeModal();
        }

        projects.push({ title: 'Comparaison Avancée DuckDB vs PostGIS', desc: 'Analyse comparative des solutions DuckDB et PostGIS dans différents scénarios de requêtes SQL spatiales', link: '' });
        renderProjects();

        const bubbleContainer = document.getElementById('bubbleContainer');
        const bubbles = Array.from(bubbleContainer.querySelectorAll('.skill-bubble'));
        
        bubbles.forEach((b, i) => {
            const tooltip = b.querySelector('.tooltip');
            const tip = b.getAttribute('data-tip');
            
            b.addEventListener('mouseenter', () => {
                tooltip.textContent = tip || '';
                tooltip.classList.add('show');
            });
            
            b.addEventListener('mouseleave', () => {
                tooltip.classList.remove('show');
            });
        });

        function updatePhoneNumber(number) { document.getElementById('phoneNumber').textContent = number; }
        
        function loadUserPhoto(url) {
            setBackgroundImage(url);
            bgOverlay.style.background = `linear-gradient(to bottom, rgba(16,16,16,0.72), rgba(16,16,16,0.96))`;
        }
        function randomFloat() {
    const bubbles = document.querySelectorAll('.skill-bubble');
    bubbles.forEach(bubble => {
        const randomX = (Math.random() - 0.5) * 20; // Random X offset
        const randomY = (Math.random() - 0.5) * 20; // Random Y offset
        bubble.style.transform = `translate(${randomX}px, ${randomY}px)`; // Apply random translation
    });
}

// Call randomFloat every few seconds to create a smooth floating effect
setInterval(randomFloat, 3000);

    </script>
</body>
</html>
